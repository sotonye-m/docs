---
title: "Apply for Issuer Program"
api: "POST /v1/card-issuer/apply"
description: "Apply to become a card issuer and configure your card program"
authMethod: "bearer"
---

<ParamField header="Authorization" type="string" required>
  Bearer token: `Bearer YOUR_ACCESS_TOKEN`
</ParamField>

<ParamField body="funding_structure" type="string" required>
  The funding model for the issuer program. One of: `master_wallet` (you hold a shared balance and push funds to customers) or `non_master` (each customer has their own collateral wallet).
</ParamField>

<ParamField body="iban_enabled" type="boolean" required>
  Whether to enable IBAN functionality for cardholders in your program.
</ParamField>

<RequestExample>
```bash
curl -X POST 'https://crypto-api.yativo.com/api/v1/card-issuer/apply' \
  -H 'Authorization: Bearer YOUR_ACCESS_TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{
    "funding_structure": "master_wallet",
    "iban_enabled": false
  }'
```
</RequestExample>

<ResponseExample>
```json
{
  "success": true,
  "message": "Application submitted successfully",
  "data": {
    "application_id": "app_69c799d9f1fba3928d8a12ac",
    "status": "pending",
    "submitted_at": "2026-03-28T10:00:00.000Z"
  }
}
```
</ResponseExample>
